#!/usr/bin/expect
set timeout 60
if {$argc == 1} {
    spawn [lindex $argv 0]
} else {
    spawn [lindex $argv 0] [lindex $argv 1]
}
exec sleep 1
expect "login:"
send "login_test\r"
expect "Password:"
send "123\r"
expect "login_test"
exec sleep 0.5
send "env && exit\r"
expect eof
